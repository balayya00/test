services:
  - type: web
    name: diary
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /ms-playwright
      - key: RENDER
        value: "true"
